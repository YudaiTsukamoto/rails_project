<h1>All users<h2>
<ol>
  <% @users.each do |user| %>
    <li><%= link_to user.name, user %>
    <% if current_user.admin? && !current_user?(user) %>
      | <%= link_to "Delete", user_path(user), method: :delete, confirme: "Are you sure?" %>
  <% end %>
    </li>
  <% end %>
</ol>
